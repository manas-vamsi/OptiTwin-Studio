import Stub from "@/components/Stub";
export default function Page() {
  return <Stub title="Define your" serif="problem" sub="Describe your operation in plain language. AI extracts resources, constraints and goals."
    note="The AI extraction UI (prompt box, file upload, editable resource/constraint/objective chips) ports here in its own PR." />;
}
