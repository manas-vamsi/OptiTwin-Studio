import Stub from "@/components/Stub";
export default function Page() {
  return <Stub title="Digital" serif="Twin" sub="A live graph of your operation. Click any node to inspect."
    note="The interactive node graph + detail drawer ports here using React Flow (added as a dependency in that PR)." />;
}
