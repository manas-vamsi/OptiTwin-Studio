import Stub from "@/components/Stub";
export default function Page() {
  return <Stub title="Scenario" serif="Simulation" sub="Stress-test the optimized schedule with what-if scenarios."
    note="Scenario library ports here — it already works end to end via lib/api.solve() with a scenario payload." />;
}
