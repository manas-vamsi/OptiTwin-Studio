import Stub from "@/components/Stub";
export default function Page() {
  return <Stub title="Generate a" serif="report" sub="Turn the latest optimization into a shareable document."
    note="Report generation (executive/optimization/technical) + CSV/PDF export ports here in its own PR." />;
}
