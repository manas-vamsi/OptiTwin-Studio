[package]
name = "optitwin-kernel"
version = "0.1.0"
edition = "2021"

[lib]
name = "optitwin_kernel"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.26", features = ["extension-module"] }

[profile.release]
lto = true
