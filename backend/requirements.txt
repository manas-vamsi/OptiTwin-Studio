fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Optional — real solvers. Install on a supported Python (3.11/3.12) to enable
# the OR-Tools classical path and Qiskit QUBO annealing. The service runs
# without them via the pure-Python fallback.
# ortools>=9.9
# qiskit-optimization>=0.6
