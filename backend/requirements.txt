fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Real solver backends (Python 3.11-3.12 wheels). The service still runs
# without them — every path falls back to the pure-Python greedy+SA solver.
ortools>=9.10
dwave-samplers>=1.3

# Persistence — solve-run history in Postgres. Optional: no DATABASE_URL or
# unreachable server just disables /api/runs; solving is unaffected.
psycopg[binary]>=3.1
